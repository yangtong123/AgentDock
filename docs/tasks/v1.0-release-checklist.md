# V1.0 Release Checklist

## Code health

- [x] TypeScript typecheck passes (`npm run typecheck`)
- [x] Full test suite green (`npm test` — 144/144)
- [x] Approval re-enqueue vs orphan-recovery race fixed; recovery contract made explicit (queue entry consumed at claim; queued ⇒ pending pickup, never orphan; crash mid-step ⇒ cancel + FAILED with atomic lease re-check). Regression tests: "an approved run queued for pickup is not orphan-recovered", "approval re-enqueue after a crash at the gate is not orphan-recovered"
- [x] `AGENTS.md` / `CLAUDE.md` milestone status matches README (V1.0)
- [x] CI runs typecheck + tests on pushes to `main` and on pull requests, on Ubuntu and macOS (`.github/workflows/ci.yml`); the macOS leg exercises the Seatbelt integration tests that Ubuntu skips

## End-to-end acceptance (real environment)

Run each item against the real stack (`npm run cli -- serve`) and record the outcome:

- [x] Real Claude Code CLI runs in isolated Git worktrees under the seatbelt sandbox (2026-08-17, run `7b451f8c` SUCCEEDED; `hello.txt` produced, deterministic VERIFY passed)
- [x] Real Codex CLI (ChatGPT.app-bundled `codex exec`) as REVIEW/FINAL_REVIEW provider in cross-review: run SUCCEEDED including a codex BLOCKER finding → claude FIX → re-review PASS loop (2026-08-17). Found a release blocker: the seatbelt profile blocked macOS trustd/securityd XPC, so codex (rustls) TLS verification failed (UnknownIssuer) and hung until step timeout. Fix: `CodexAgent` sets `SSL_CERT_FILE=/etc/ssl/cert.pem` so rustls uses file-based roots — the Seatbelt profile grants zero mach-lookup broker access (no keychain-adjacent XPC surface)
- [x] `careful` preset: both approval gates exercised via the IM approval path (approve → re-enqueue → orchestrator resume) — 2026-08-17
- [x] Cancel mid-run: run and steps settle to CANCELLED and are never overwritten by the in-flight step result; no partial worktree state (2026-08-17). Note: CLI cancel is cooperative (DB state); the owner-scoped process-tree kill path (`orchestrator.requestCancel`, IM `/stop`) is covered by unit tests but was not IM-exercised locally
- [x] Service crash mid-run (`kill -9` of the lease-holding worker, 2026-08-17): after lease expiry the restarted worker's recovery (full predicate re-checked inside the write transaction) cancels the run and fails the task — `run=CANCELLED`, `task=FAILED`, `task.orphan-recovered` event published; no mid-step resume. Retry: GitHub fix triggers re-open automatically via the fix sweep; plain tasks have no generic FAILED→READY retry entry — recovery leaves them visibly FAILED for a human to re-run (candidate for post-V1.0). Crash at an approval gate is excluded from orphan classification, and a post-crash approval re-enqueue resumes normally (regression tests cover both). Known boundary: the crashed worker's detached agent process survives as an orphan and can keep writing to the worktree until it exits — agents should run under a dedicated macOS user in production. Graceful shutdown (SIGTERM) exits cleanly
- [ ] Telegram: `/projects`, `/use`, `/new`, `/run`, `/status`, `/diff`, `/stop`, approval buttons (needs `TELEGRAM_BOT_TOKEN`)
- [ ] Feishu/Lark: same command surface; a task created in Telegram is controllable from Feishu and vice versa (needs Feishu credentials)
- [ ] GitHub: branch push + Draft PR creation (never auto-merges) — `gh` is authenticated; creates a real PR, do it on a throwaway repo
- [ ] CI failure on the PR ingested via `github refresh` → fix workflow runs → PR updated
- [ ] Budget cap and audit log entries via `metrics` / `audit` CLI: per-step durations and APPROVE_RUN audit rows verified 2026-08-17, but token fields are null in usage rows (claude JSON usage is not parsed into them) — token-cost tracking is not available until this is fixed; item stays unchecked

## Release mechanics

- [ ] CI green on `main`
- [ ] `package.json` version is `1.0.0`
- [ ] Tag `v1.0.0` on the release commit and push the tag
- [ ] README quick-start verified against a fresh clone

## Post-release

- [ ] Run AgentDock as the daily driver; track success rate, task duration, token cost (blocked on the usage-parsing gap above), and manual interventions
- [ ] Decide V1.1 scope (candidate: web dashboard) from that usage data, not speculation
- [ ] Known gap (pre-existing, non-blocking): task-timeout/lease-loss kills are one-shot — a timeout firing in the sub-millisecond window between steps lets the next step's process spawn and the run continue past the hard timeout
