# V1.1 Release Checklist — Cross-Agent Workbench

## Code health

- [x] TypeScript typecheck passes (`npm run typecheck`)
- [x] Full root test suite green (`npm test`)
- [x] Workbench typecheck + build green (`npm run build:workbench`)
- [x] Workbench unit tests green (`npm --prefix workbench test` — vitest)
- [x] Workbench Playwright e2e green (`npm --prefix workbench run e2e` — 5 specs: token gate, composer + cross-review with findings, careful approval ×2, refresh restores run detail, cancel from UI)
- [x] Secret-redaction sweep: gateway responses/SSE/error paths never serialize orchestrator credentials (`tests/security.test.ts` sentinel test)
- [x] CI runs typecheck + root tests + workbench build/vitest on Ubuntu and macOS; Playwright e2e on Ubuntu (`.github/workflows/ci.yml`)

## Milestone acceptance coverage

- [x] M1: gateway REST + SSE, idempotency keys, activity stream, shared command layer (`tests/gateway.test.ts`, `tests/sse.test.ts`, `tests/commands.test.ts`, `tests/activity.test.ts`)
- [x] M2: read-only workbench — list + run detail, virtualized bounded logs, capped diffs, per-file review findings (`workbench/src`, e2e specs)
- [x] M3: composer + desktop controls with per-intent idempotency keys, expectedRunState conflicts, swap-and-rerun (`tests/contract.test.ts`, `tests/commands.test.ts`)
- [x] M4: cross-channel — IM provider assignment, `/providers`, actor contract (desktop/telegram/feishu/cli/system), approval/cancel notifications per subscribed conversation, concurrent-decision conflicts (`tests/contract.test.ts`, `tests/im.test.ts`)
- [x] M5 reliability: slow-SSE-client cap + non-stall proof, SSE cursor survives DB reopen (`tests/sse.test.ts`)

## End-to-end acceptance (real environment)

Run each item against the real stack (`npm run cli -- serve`, open the gateway URL with `?token=` from `.agentdock/gateway-token`) and record the outcome:

- [x] **Real workbench run (single provider)** — 2026-08-19, real `claude` CLI 2.1.220 on macOS, isolated scratch repo + DB (`node --test` verify command, `default` permission profile). Composer → preset `cross-review` → "All Claude" → task `5cbfaaab` run 1 SUCCEEDED end-to-end: IMPLEMENT 38.1s, VERIFY 71ms (`node --test` TAP 2/2 pass, rendered in the deterministic-verification section), REVIEW 2m54s (`FINDING (none)` / `VERDICT: PASS`), FIX, VERIFY, FINAL_REVIEW 2m4s (`VERDICT: PASS`). Per-file diff, artifacts, revisions, and the activity timeline (actors `desktop`/`system`) all rendered; steps progressed live over SSE without refresh; the step-log pane tailed real agent output. Findings rendering itself is covered by the fake-agent e2e spec (`composer.spec.ts`); this run's reviews returned clean PASS with no findings to render. Observed: `git` subprocesses fail inside the seatbelt write-jail (`could not open '/dev/null'`); agents worked around it by reading the worktree directly — candidate follow-up for the sandbox profile.
- [x] **2026-08-19 (evening) — Claude-implement/Codex-review via the workbench**: task `68759d8a`, composer → preset `cross-review` → quick combo "Claude Build / Codex Review" → run 1 SUCCEEDED, all 6 steps: IMPLEMENT (claude) 38s-class, VERIFY (`node --test` green), REVIEW (**codex**, DeepSeek provider) `VERDICT: PASS`, FIX (claude), VERIFY, FINAL_REVIEW (**codex**) SUCCEEDED. Setup notes: standalone codex CLI 0.148.0 installed via npm (the ChatGPT.app-bundled alias does not resolve in child processes), provider switched to DeepSeek in `~/.codex/config.toml` (`model_provider = "deepseek"`, inline bearer token — no env-var dependency, so AgentDock's env isolation is compatible). codex ran under the OS write-jail with its native sandbox disabled, as designed.
- [x] **2026-08-19 (evening) — Codex-implement/Claude-review via the workbench**: task `6fde9a7c`, quick combo "Codex Build / Claude Review" → run 1 SUCCEEDED, all 6 steps: IMPLEMENT (**codex**, real code: `divide` with `RangeError` on zero + 3 test cases), VERIFY green, REVIEW (claude) `VERDICT: PASS` (reviewer honestly noted the tool allowlist prevented it from executing `node --test` — inspection-only verdict), FIX (codex), VERIFY, FINAL_REVIEW (claude) `VERDICT: PASS`. Final `node --test` in the worktree: 4/4 pass.
- [x] **2026-08-19 (evening) — cross-channel, desktop start + Feishu approval**: task `b7b284cb`, workbench composer → preset `careful` (Claude plan/implement, Codex review/final). PLAN done → gate 1 approved from Feishu (`/approve 7c979df7-…`, which also subscribed the conversation via `im_task_subscriptions`) → IMPLEMENT/VERIFY/REVIEW/FIX/VERIFY/FINAL_REVIEW all SUCCEEDED → the **final gate auto-notified Feishu** (`approval.requested` delivered to the subscribed conversation) → approved from Feishu → run SUCCEEDED, `run.succeeded` terminal notification delivered to Feishu. Desktop workbench showed every state change live over SSE. Code verified: `power()` + 3/3 tests green. **Finding**: a desktop-started task has no IM subscriber until first IM interaction (`/run`/`/approve` create the subscription), so its *first* approval gate notifies no conversation — the user must learn about it from the workbench. Candidate follow-up: notify project-origin conversations or add a `/watch` command.
- [x] **2026-08-19 (evening) — cross-channel, Feishu start + desktop observation**: task `0717432b`, IM commands `/use accept-demo` → `/new <requirement>` → `/run <taskId> cross-review IMPLEMENT=codex REVIEW=claude` (per-step provider assignment from IM). The task appeared in the desktop workbench **without a manual refresh**, the activity timeline recorded `task.created`/`run.queued` with actor `feishu`, and the run detail showed codex implementing (live log pane, 1s poll) with claude reviewing. Run 1 SUCCEEDED (all 6 steps); terminal notification delivered to Feishu; final `node --test` in the worktree: 2/2 pass.

## Release mechanics

- [ ] CI green on `main` (ubuntu + macOS root suites, ubuntu workbench e2e)
- [x] README quick-start verified against a fresh clone (serve → gateway URL + token → workbench) — 2026-08-19: fresh clone of `dfc644a` showed the README install section was incomplete (root `npm install` + `npm run build` alone leave the workbench 404 "workbench is not built"; workbench deps must be installed too). README fixed in the same pass; verified working with the full four-step install, gateway 200 + SPA HTML.

## Post-release

- [ ] Daily-driver the workbench; watch SSE reconnect behavior over laptop sleep and gateway restarts
